//Задача 1




















//Задача 2

class Triangle {
	constructor (a, b, c) {
		func(a, b,c) {
			if (a + b < c || b + c < a || a + c < b) {
				//исключение "Треугольник с такими сторонами не существует".
			}
		}
	
	getPerimeter() {
		let perimeter = a + b + c;
		return console.log("Периметр треуголиника: " + perimeter)
	}

	getArea() {
		let halfPerimeter = perimeter / 2;
	}
}




























